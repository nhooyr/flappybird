first input -> start game -> game loop.move bird

game loop: {
  input -> increase bird X velocity

  move bird -> move pipes -> render

  render -> no collision -> wait 16 milliseconds -> move bird
  render -> collision detected -> _.game over
}
